{
  "name": "infernosource-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "ioredis": "^5.6.1"
  }
}
